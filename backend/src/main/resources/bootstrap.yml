spring:
  config:
    import:
      # backend 키 이름
      - "aws-secretsmanager:${BACKEND_SECRET_NAME}"
      # rds 키 이름
      - "aws-secretsmanager:${RDS_SECRET_NAME}"

  cloud:
    aws:
      region:
        static: ap-northeast-2
